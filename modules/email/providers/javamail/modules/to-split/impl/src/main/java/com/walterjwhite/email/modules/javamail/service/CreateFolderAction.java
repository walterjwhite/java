package com.walterjwhite.email.modules.javamail.service;

public class CreateFolderAction {
  //    Folder newFolder = folder.getFolder("testing");
  //    if (!newFolder.exists()) {
  //      if (newFolder.create(Folder.HOLDS_MESSAGES + Folder.HOLDS_FOLDERS)) {
  //        newFolder.setSubscribed(true);
  //        LOGGER.info("Folder was created successfully");
  //      } else {
  //        LOGGER.info("Folder was NOT created successfully?");
  //      }
  //    }
}
