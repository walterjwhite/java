package com.walterjwhite.remote.modules.cli.handler;

import com.walterjwhite.inject.cli.service.DaemonCommandLineHandler;

/** Run in client mode (default operation). */
public class RemoteDaemon implements DaemonCommandLineHandler {

  //  @Inject
  //  public RemoteDaemon(
  //      @Property(CommandLineHandlerShutdownTimeout.class) int shutdownTimeoutInSeconds) {
  //    super(shutdownTimeoutInSeconds);
  //  }
}
